[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "atlantic-policy-rag"
version = "0.1.0"
description = "Atlantic Ventures policy RAG API (hybrid retrieval over corporate policy docs)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "qdrant-client",
    "fastembed",
    "sentence-transformers",
    "google-genai",
    "python-dotenv",
    "pydantic",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*", "backend*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "integration: needs Qdrant (and optionally local embedding model download)",
    "live: runs real FastEmbed model (set RUN_LIVE_EMBEDDING=1)",
]
